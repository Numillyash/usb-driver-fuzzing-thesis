from __future__ import annotations


def decode_text_line(line: str) -> str:
    """
    Placeholder for future structured log decoding.

    Today the console only deals with plain text lines coming from the REPL
    fallback or bridge bring-up messages.
    """
    return f"rx: {line}"
